version: '3.7'

services:
  db:
    image: postgres
    container_name: db
    environment:
      - POSTGRES_USER=anas
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=nest
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql/data
    networks:
      - ft_transendance

volumes:
  data:

networks:
  ft_transendance:
    driver: bridge
